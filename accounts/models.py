# Ldrbrd uses Django's stock User model.  The instructor bit is is_staff, and
# Canvas identity lives in allauth's SocialAccount, so there is nothing to add
# here.  The module exists so Django's app loader stays happy.
